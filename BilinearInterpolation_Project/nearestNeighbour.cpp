//
// Created by Sebastian on 19.01.2020.
//

#include "imageutil.h"
#include <cmath>

 void scaleImageNearestNeigbour(png_user_struct *oldImage, png_user_struct *scaledImage, double scalingFactor) {
//     ImageBuffer *newImgBuf = &scaledImage->imageBuffer;
//     for (unsigned int y = 0; y <= scaledImage->image_info.height; y++) {
//
//         unsigned int yScaled = (unsigned int) round((double) y / scalingFactor);
//
//         for (unsigned int x = 0; x <= scaledImage->image_info.width; x++) {
//
//             unsigned int xScaled = (unsigned int) round((double) x / scalingFactor);
//
//             newImgBuf->buffer[y][x] = oldImage->imageBuffer.buffer[yScaled][xScaled];
//
//         }
//     }
}